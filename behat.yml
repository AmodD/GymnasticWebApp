default:
    autoload:
        '': "%paths.base%/bdd/contexts"  
    extensions:
        Laracasts\Behat\ServiceContainer\BehatExtension: ~
        Laracasts\Behat:
            env_path: .env.behat
        Behat\MinkExtension:
            default_session: laravel
            base_url: http://localhost
            laravel: ~
    gherkin:
        filters:
            tags: ~@wip
    suites:
        pages:
            paths:    [ "%paths.base%/bdd/features/pages" ]        
            contexts: [ FeatureContext ] 
        attendance:
            paths:    [ "%paths.base%/bdd/features/attendance" ]        
            contexts: [ FeatureContext ] 
        students:
            paths:    [ "%paths.base%/bdd/features/students" ]        
            contexts: [ StudentContext ] 
        batch_students:
            paths:    [ "%paths.base%/bdd/features/students" ]        
            contexts: [ BatchContext ] 
